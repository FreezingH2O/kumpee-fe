export function SiteFooter() {
  return (
    <footer className="border-t border-line bg-surface">
      <div className="mx-auto flex max-w-[1280px] items-center justify-end px-4 py-4 lg:px-8">
        <p className="flex items-center gap-2 text-sm text-ink-400">
          <span className="h-[3px] w-5 rounded-chip bg-primary-600" aria-hidden />
          ภาษาดี ทำให้โลกน่าอยู่ขึ้น
        </p>
      </div>
    </footer>
  );
}
